# Termos do exemplo ZIP

Este exemplo baixa um arquivo `package.zip`, extrai `wizard.js` em memoria e executa o script dentro do runtime do Client Wizard.

O pacote nao instala software real. Ele apenas consulta informacoes basicas do sistema depois que a permissao `native:systemInfo` for aprovada.
