## Final fix
- Narrowed the `link` UI event to dynamic JSON `href` targets only.
- Documented that normal HTTP links open externally and local `.md` links load through the host without emitting `wizard.events()`.
- Verified the guide now contains the requested wording.
